from __future__ import annotations

from pathlib import Path

PROJECT_FILES = {
    "app_main.py": "from __future__ import annotations\n\nfrom typing import Any\n\nimport streamlit as st\n\nfrom db import init_db\nfrom services.background_scheduler import ensure_scheduler_started, get_scheduler_next_run\nfrom services.briefing_service import BriefingService\nfrom utils.time_utils import get_reference_trade_date, now_kst, to_display_datetime\n\n\nPAGE_TITLE = \"장전 단타 브리핑\"\nDEFAULT_CANDIDATE_COUNT = 3\nDEFAULT_MIN_SCORE = 75\n\n\ndef current_trade_date() -> str:\n    return get_reference_trade_date(now_kst())\n\n\ndef init_page() -> None:\n    st.set_page_config(page_title=PAGE_TITLE, page_icon=\"📈\", layout=\"wide\", initial_sidebar_state=\"collapsed\")\n    st.markdown(\n        \"\"\"\n        <style>\n        .main {\n            background:\n                radial-gradient(circle at top right, rgba(38, 166, 154, 0.10), transparent 28%),\n                radial-gradient(circle at top left, rgba(255, 183, 77, 0.10), transparent 22%),\n                #f6f7f9;\n        }\n        .block-container { padding-top: 2rem; padding-bottom: 3rem; max-width: 1180px; }\n        .hero-card, .info-card, .sub-card, .log-card {\n            background: white;\n            border-radius: 20px;\n            padding: 1.4rem 1.5rem;\n            border: 1px solid rgba(15, 23, 42, 0.08);\n            box-shadow: 0 16px 40px rgba(15, 23, 42, 0.06);\n        }\n        .hero-card { background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%); color: white; }\n        .hero-title { font-size: 1.9rem; font-weight: 800; margin-bottom: 0.3rem; }\n        .hero-subtitle { color: rgba(255,255,255,0.78); margin-bottom: 1rem; }\n        .metric-label { font-size: 0.82rem; color: #64748b; margin-bottom: 0.2rem; }\n        .hero-card .metric-label { color: rgba(255,255,255,0.70); }\n        .metric-value { font-size: 1.1rem; font-weight: 700; color: #0f172a; }\n        .hero-card .metric-value { color: white; }\n        .status-badge {\n            display: inline-block;\n            padding: 0.35rem 0.7rem;\n            border-radius: 999px;\n            font-size: 0.82rem;\n            font-weight: 700;\n            margin-bottom: 0.9rem;\n        }\n        .status-buy { background: rgba(22, 163, 74, 0.15); color: #15803d; }\n        .status-wait { background: rgba(59, 130, 246, 0.14); color: #1d4ed8; }\n        .status-no { background: rgba(239, 68, 68, 0.14); color: #b91c1c; }\n        .status-chase { background: rgba(245, 158, 11, 0.16); color: #b45309; }\n        .price-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 0.8rem 1.2rem; margin-top: 1rem; }\n        .sub-card-title { font-size: 1.15rem; font-weight: 800; margin-bottom: 0.45rem; color: #0f172a; }\n        .reason-text { font-size: 0.95rem; line-height: 1.45; color: #475569; }\n        .empty-box { border: 1px dashed #cbd5e1; border-radius: 18px; padding: 1.4rem; background: rgba(255,255,255,0.55); color: #475569; }\n        .news-link-box { margin-top: 1rem; padding-top: 1rem; border-top: 1px solid rgba(255,255,255,0.14); }\n        .news-link-box a { color: #bfdbfe; text-decoration: none; }\n        .news-link-box a:hover { text-decoration: underline; }\n        </style>\n        \"\"\",\n        unsafe_allow_html=True,\n    )\n\n\ndef badge_class(status: str) -> str:\n    mapping = {\n        \"지금 매수 가댥\": \"status-buy\",\n        \"눌림 대기\": \"status-wait\",\n        \"제외\": \"status-no\",\n        \"추격 금지\": \"status-chase\",\n    }\n    return mapping.get(status, \"status-wait\")\n\n\ndef format_price_range(item: dict[str, Any]) -> str:\n    if item[\"buy_min\"] == item[\"buy_max\"]:\n        return f\"{item['buy_min']:,.0f}원\"\n    return f\"{item['buy_min']:,.0f}원 ~ {item['buy_max']:,.0f}원\"\n\n\ndef render_news_links(news_items: list[dict[str, Any]], dark: bool = False) -> None:\n    if not news_items:\n        return\n    if dark:\n        html = ['<div class=\"news-link-box\"><div class=\"metric-label\">확인용 뉴스 링크</div>']\n        for item in news_items[:3]:\n            html.append(\n                f\"<div style='margin-top:0.35rem;'><a href=\\\"{item['url']}\\\" target=\\\"_blank\\\">\"\n                f\"[{item['source']}] {item['title']}</a> · 강도 {item.get('news_strength', 0):.1f}</div>\"\n            )\n        html.append(\"</div>\")\n        st.markdown(\"\".join(html), unsafe_allow_html=True)\n        return\n    st.caption(\"확인용 뉴스 링크\")\n    for item in news_items[:3]:\n        st.markdown(f\"- [[{item['source']}] {item['title']}]({item['url']}) · 강도 {item.get('news_strength', 0):.1f}\")\n\n\ndef render_scheduler_status(scheduler_status: dict[str, Any]) -> None:\n    st.markdown(\"### 자동 수집 상태\")\n    c1, c2, c3 = st.columns(3)\n    c1.markdown(\n        f'<div class=\"log-card\"><div class=\"metric-label\">최근 실행 상태</div><div class=\"metric-value\">{scheduler_status.get(\"status\", \"idle\")}</div></div>',\n        unsafe_allow_html=True,\n    )\n    c2.markdown(\n        f'<div class=\"log-card\"><div class=\"metric-label\">최근 완료 시각</div><div class=\"metric-value\">{to_display_datetime(scheduler_status[\"finished_at\"]) if scheduler_status.get(\"finished_at\") else \"-\"}</div></div>',\n        unsafe_allow_html=True,\n    )\n    c3.markdown(\n        f'<div class=\"log-card\"><div class=\"metric-label\">다음 예정 시각</div><div class=\"metric-value\">{to_display_datetime(scheduler_status[\"next_run_at\"]) if scheduler_status.get(\"next_run_at\") else \"-\"}</div></div>',\n        unsafe_allow_html=True,\n    )\n    if scheduler_status.get(\"message\"):\n        st.caption(scheduler_status[\"message\"])\n\n\ndef render_header(briefing: dict[str, Any], scheduler_status: dict[str, Any]) -> None:\n    st.markdown(f\"## {PAGE_TITLE}\")\n    c1, c2, c3, c4 = st.columns(4)\n    c1.metric(\"기준 일자\", briefing[\"trade_date\"])\n    c2.metric(\"생성 시각\", to_display_datetime(briefing[\"created_at\"]))\n    c3.metric(\"추천 종목 수\", str(len(briefing[\"recommendations\"])))\n    c4.metric(\"최소 기준 점수\", f\"{briefing['min_score']:.0f}점\")\n    logs = briefing.get(\"logs\", {})\n    st.caption(\n        \"뉴스/시세/공시는 브리핑 생성 시점 기준으로 저장되며, \"\n        f\"백그라운드에서 3시간마다 자동 갱신됹니다. \"\n        f\"마지막 수집 소스: 시장 {logs.get('market_source', 'unknown')} / \"\n        f\"뉴스 {logs.get('news_source', 'unknown')} / \"\n        f\"공시 {logs.get('disclosure_source', 'unknown')}\"\n    )\n    render_scheduler_status(scheduler_status)\n\n\ndef render_top_pick(top_pick: dict[str, Any] | None) -> None:\n    st.markdown(\"### 내일의 최고 후보 1위\")\n    if not top_pick:\n        st.markdown('<div class=\"empty-box\">오늘은 기준을 넘는 종목이 없어 추천을 비웠습니다.</div>', unsafe_allow_html=True)\n        return\n    st.markdown(\n        f\"\"\"\n        <div class=\"hero-card\">\n            <div class=\"hero-title\">{top_pick['ticker_name']} <span style=\"font-size:1rem; font-weight:600;\">({top_pick['ticker']})</span></div>\n            <div class=\"hero-subtitle\">{top_pick['plan_type']} · 추천 점수 {top_pick['total_score']:.1f}점</div>\n            <div class=\"status-badge {badge_class(top_pick['status'])}\">{top_pick['status']}</div>\n            <div style=\"font-size:1.08rem; font-weight:700; margin-bottom:0.45rem;\">{top_pick['action_guide']}</div>\n            <div class=\"reason-text\" style=\"color:rgba(255,255,255,0.84); margin-bottom:1rem;\">{top_pick['reason_text']}</div>\n            <div class=\"price-grid\">\n                <div><div class=\"metric-label\">매수 구간</div><div class=\"metric-value\">{format_price_range(top_pick)}</div></div>\n                <div><div class=\"metric-label\">손절 가격</div><div class=\"metric-value\">{top_pick['stop_loss']:,.0f}원</div></div>\n                <div><div class=\"metric-label\">1차 매도</div><div class=\"metric-value\">{top_pick['take_profit_1']:,.0f}원</div></div>\n                <div><div class=\"metric-label\">최종 매도</div><div class=\"metric-value\">{top_pick['take_profit_2']:,.0f}원</div></div>\n            </div>\n        </div>\n        \"\"\",\n        unsafe_allow_html=True,\n    )\n    render_news_links(top_pick.get(\"related_news\", []), dark=True)\n    if top_pick.get(\"material_reasons\"):\n        st.caption(\"점수 반영: \" + \" | \".join(top_pick[\"material_reasons\"]))\n    if top_pick.get(\"risk_notes\"):\n        st.caption(\"감점 요인: \" + \" | \".join(top_pick[\"risk_notes\"]))\n\n\ndef render_secondary_picks(picks: list[dict[str, Any]]) -> None:\n    st.markdown(\"### 차순위 후보\")\n    if not picks:\n        st.markdown('<div class=\"empty-box\">차순위 후보는 따로 남기지 않았습니다.</div>', unsafe_allow_html=True)\n        return\n    columns = st.columns(min(2, len(picks)))\n    for index, item in enumerate(picks):\n        with columns[index % len(columns)]:\n            st.markdown(\n                f\"\"\"\n                <div class=\"sub-card\">\n                    <div class=\"sub-card-title\">{item['ticker_name']} <span style=\"font-size:0.92rem; color:#64748b;\">({item['ticker']})</span></div>\n                    <div class=\"status-badge {badge_class(item['status'])}\">{item['status']}</div>\n                    <div class=\"reason-text\" style=\"margin-bottom:0.9rem;\">{item['reason_text']}</div>\n                    <div class=\"metric-label\">매수 구간</div>\n                    <div class=\"metric-value\" style=\"margin-bottom:0.7rem;\">{format_price_range(item)}</div>\n                    <div class=\"metric-label\">손절 가격</div>\n                    <div class=\"metric-value\" style=\"margin-bottom:0.7rem;\">{item['stop_loss']:,.0f}원</div>\n                    <div class=\"metric-label\">1차 매도 / 최종 매도</div>\n                    <div class=\"metric-value\">{item['take_profit_1']:,.0f}원 / {item['take_profit_2']:,.0f}원</div>\n                </div>\n                \"\"\",\n                unsafe_allow_html=True,\n            )\n            render_news_links(item.get(\"related_news\", []), dark=False)\n            if item.get(\"material_reasons\"):\n                st.caption(\"점수 반영: \" + \" | \".join(item[\"material_reasons\"]))\n            if item.get(\"risk_notes\"):\n                st.caption(\"감점 요인: \" + \" | \".join(item[\"risk_notes\"]))\n\n\ndef render_exclusion_notes(notes: list[str]) -> None:\n    note_text = \"<br>\".join(f\"• {note}\" for note in notes) if notes else \"• 제외 메모 없음\"\n    st.markdown(\"### 제외 메모\")\n    st.markdown(f'<div class=\"info-card\">{note_text}</div>', unsafe_allow_html=True)\n\n\ndef render_logs(logs: dict[str, Any]) -> None:\n    st.markdown(\"### 추천 생성 로그\")\n    c1, c2, c3, c4 = st.columns(4)\n    c1.markdown(f'<div class=\"log-card\"><div class=\"metric-label\">뉴스 수집 건수</div><div class=\"metric-value\">{logs[\"news_count\"]}</div></div>', unsafe_allow_html=True)\n    c2.markdown(f'<div class=\"log-card\"><div class=\"metric-label\">공시 수집 건수</div><div class=\"metric-value\">{logs[\"disclosure_count\"]}</div></div>', unsafe_allow_html=True)\n    c3.markdown(f'<div class=\"log-card\"><div class=\"metric-label\">최종 분석 대상</div><div class=\"metric-value\">{logs[\"analyzed_count\"]}</div></div>', unsafe_allow_html=True)\n    c4.markdown(f'<div class=\"log-card\"><div class=\"metric-label\">최종 추천 종목 수</div><div class=\"metric-value\">{logs[\"recommended_count\"]}</div></div>', unsafe_allow_html=True)\n    st.caption(f\"시장 덳이터: {logs.get('market_source', 'unknown')} | 뉴스: {logs.get('news_source', 'unknown')} | 공시: {logs.get('disclosure_source', 'unknown')}\")\n\n\ndef render_news_collection(briefing: dict[str, Any]) -> None:\n    st.markdown(\"### 수집된 뉴스\")\n    news_rows = briefing.get(\"collected_news\", [])\n    if not news_rows:\n        st.markdown('<div class=\"empty-box\">이번 브리핑에 저장된 뉴스가 없습니다.</div>', unsafe_allow_html=True)\n        return\n    table_rows = [\n        {\n            \"시각\": to_display_datetime(item[\"published_at\"]),\n            \"강도\": item[\"news_strength\"],\n            \"출처\": item[\"source\"],\n            \"관련 종목\": \", \".join(item.get(\"related_tickers\", [])),\n            \"제목\": item[\"title\"],\n        }\n        for item in news_rows[:30]\n    ]\n    st.dataframe(table_rows, use_container_width=True, hide_index=True)\n    with st.expander(\"수집 뉴스 원문 링크와 요약 보기\", expanded=False):\n        for item in news_rows[:30]:\n            st.markdown(f\"**[{item['source']}] [{item['title']}]({item['url']})**\")\n            st.caption(\n                f\"발행 {to_display_datetime(item['published_at'])} | 강도 {item['news_strength']:.1f} | \"\n                f\"연결 종목 {', '.join(item.get('related_tickers', []))}\"\n            )\n            if item.get(\"summary\"):\n                st.write(item[\"summary\"])\n            st.divider()\n\n\ndef render_review_section(service: BriefingService, briefing: dict[str, Any]) -> None:\n    review_rows = briefing.get(\"score_review\") or service.get_score_review(briefing[\"trade_date\"], limit=15)\n    st.markdown(\"### 검토한 종목 보기\")\n\n    if not review_rows:\n        st.markdown('<div class=\"empty-box\">이번 브리핑에서 저장된 검토 종목 덳이터가 없습니다.</div>', unsafe_allow_html=True)\n        return\n\n    recommended_count = len(briefing[\"recommendations\"])\n    if recommended_count == 0:\n        st.info(\"추천 후보가 0개인 이유는 고장이라기보다, 검토한 종목 중 최소 추천 점수를 넘은 종목이 없었기 때문입니다. 아래에서 점수 순위와 감점 사유를 확인할 수 있습니다.\")\n\n    summary_rows = [\n        {\n            \"순위\": row[\"rank\"],\n            \"종목\": row.get(\"ticker_name\") or row[\"ticker\"],\n            \"코드\": row[\"ticker\"],\n            \"총점\": row[\"total_score\"],\n            \"재료\": row[\"material_score\"],\n            \"수급\": row[\"money_flow_score\"],\n            \"자리\": row[\"setup_score\"],\n            \"감점\": row[\"risk_penalty\"],\n            \"등락률\": row.get(\"change_rate\", 0.0),\n        }\n        for row in review_rows\n    ]\n    st.dataframe(summary_rows, use_container_width=True, hide_index=True)\n\n    with st.expander(\"점수 산정 근거 상세 보기\", expanded=False):\n        for row in review_rows:\n            st.markdown(\n                f\"**{row['rank'